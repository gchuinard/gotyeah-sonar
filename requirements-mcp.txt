# Dépendances du serveur MCP LOCAL (sonar_mcp).
# NE PAS ajouter à requirements.txt : le MCP tourne sur ta machine (Claude Desktop /
# Claude Code), pas sur le Pi. L'image Docker de Sonar n'a donc aucune dépendance en plus.
#
#   python3 -m venv .mcpvenv && . .mcpvenv/bin/activate
#   pip install -r requirements-mcp.txt
#
mcp>=1.27,<2
httpx>=0.27
