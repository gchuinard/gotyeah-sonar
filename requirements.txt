fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
dnspython>=2.6
PyYAML>=6.0
# MCP DISTANT (claude.ai web) — serveur OAuth + Streamable HTTP monté dans l'app.
# Contrairement au MCP stdio local (requirements-mcp.txt), celui-ci tourne SUR le
# serveur Sonar (Pi/Docker) : sa dépendance va donc dans l'image. Inerte tant que
# SONAR_MCP_REMOTE n'est pas activé (import paresseux).
mcp>=1.27,<2
